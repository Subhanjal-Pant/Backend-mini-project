<!DOCTYPE html>
<html lang="en">
<head>

    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Index</title>
    <script src="https://unpkg.com/@tailwindcss/browser@4"></script>

</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white flex flex-col">
        <h1 class="text-3xl m-3 font-bold">User Profile</h1>
        
        <div class="w-full min-h-screen mx-10 my-6 flex flex-col gap-2">
            <p class="text-[18px]">Hello <%= username %>  👋</p>
            <p>You can create a new post.</p>
            <form action="/userProfilePost" method="post" class="flex flex-col gap-2">
                <textarea class="w-96 h-40" name="post_area_content" id="01" placeholder="What's on your mind?"></textarea>
                <button class="bg-blue-500 active:scale-95 active:bg-blue-600 w-40 font-bold p-1" type="submit">Create New Post</button>
            </form>
            <div class="posts flex flex-col flex-wrap gap-4">
                <h2 class="text-zinc-400">Your Posts:</h2>
                <div class="flex flex-col">
                    <h4 class="text-blue-500">@<%= username %></h4>
                    <p class="w-96"><%= postContent  %></p>
                </div>
                <div class="flex flex-row gap-6">
                    <a class="text-blue-500" href="">Like</a>
                    <a class="text-zinc-400" href="">Edit Post</a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>